import {input,num,clamp,layoutValue,toast} from './editor-helpers.js';
const anchors=['top-left','top-center','top-right','center-left','center','center-right','bottom-left','bottom-center','bottom-right'];
function block(name,key,l={}){return `<fieldset><legend>${name}</legend><div class="form-grid"><div class="field"><label>Anchor</label><select data-k="${key}" data-f="anchor">${anchors.map(a=>`<option ${a===l.anchor?'selected':''}>${a}</option>`).join('')}</select></div>${input('X offset (vw)',l.x??0,'number',`data-k="${key}" data-f="x" min="-50" max="50" step="0.5"`)}${input('Y offset (vh)',l.y??0,'number',`data-k="${key}" data-f="y" min="-50" max="50" step="0.5"`)}${input('Scale',l.scale??1,'number',`data-k="${key}" data-f="scale" min="0.25" max="3" step="0.05"`)}${input('Background opacity',l.bgOpacity??.5,'number',`data-k="${key}" data-f="bgOpacity" min="0" max="1" step="0.05"`)}</div></fieldset>`}
export function renderVisualPanel(p,ctx){const e=ctx.state.event;p.innerHTML=`<h2 class="section-title">Clock & Countdown</h2><p class="section-sub">Each HUD can be anchored independently, scaled from 25%–300%, then fine-positioned with viewport-relative X/Y offsets.</p><div class="hud-grid">${block('Event Clock','clockLayout',e.clockLayout)}${block('Countdown','countdownLayout',e.countdownLayout)}</div>${input('Countdown label',e.countdownLabel||'Ceremony Begins In','text','id="countdownLabel"')}<div class="toolbar"><button class="btn primary" id="saveVisual">Save clock/countdown layout</button></div>`;p.querySelector('#saveVisual').onclick=async()=>{const read=k=>{const q=f=>layoutValue(p,k,f);return{anchor:q('anchor').value,x:clamp(num(q('x').value),-50,50),y:clamp(num(q('y').value),-50,50),scale:clamp(num(q('scale').value,1),.25,3),bgOpacity:clamp(num(q('bgOpacity').value,.5),0,1)}};await ctx.backend.updateEvent(ctx.eventId,{clockLayout:read('clockLayout'),countdownLayout:read('countdownLayout'),countdownLabel:p.querySelector('#countdownLabel').value.trim().slice(0,100)});toast('HUD layout saved')}}
