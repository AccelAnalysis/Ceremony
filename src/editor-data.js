import {input,notice,toast} from './editor-helpers.js';
import {buildSeedPayload} from './seed-data.js';
import {getFirebaseConfig,saveFirebaseOverride} from './config-store.js';
export function renderDataPanel(p,ctx){const cfg=getFirebaseConfig(),event=ctx.state.event;p.innerHTML=`<h2 class="section-title">Setup / Data</h2>${notice(`Event ID: <b>${ctx.eventId}</b> · Backend: <b>${ctx.demoMode?'Demo / local':'Firebase'}</b>`)}<div class="toolbar"><button class="btn primary" id="seed">${event?'Reset to AK9I starter deck':'Create AK9I starter event'}</button><button class="btn" id="export">Export event JSON</button></div><div class="divider"></div><h3>Firebase web app</h3><p class="muted">Project <b>ceremony-d1618</b>. After the Firebase web app named <b>Ceremony</b> is registered, this device needs its public Web API key and App ID.</p><div class="form-grid">${input('API key',cfg.apiKey||'','text','id="apiKey"')}${input('App ID',cfg.appId||'','text','id="appId"')}</div><div class="toolbar"><button class="btn" id="saveFirebase">Save config on this device</button></div>`;p.querySelector('#seed').onclick=async()=>{if(event&&!confirm('Reset this event to the starter deck?'))return;await ctx.backend.seedEvent(ctx.eventId,buildSeedPayload(event?.name||'AK9I Graduation'));toast('Starter event ready')};p.querySelector('#export').onclick=()=>{const blob=new Blob([JSON.stringify(ctx.state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${ctx.eventId}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};p.querySelector('#saveFirebase').onclick=()=>{saveFirebaseOverride({apiKey:p.querySelector('#apiKey').value.trim(),appId:p.querySelector('#appId').value.trim()});location.reload()}}
